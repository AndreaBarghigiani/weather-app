import clsx from "clsx";

function CitySearch({ className }) {
  const classes = clsx(className);
  return (
    <div className={classes}>
      <p>This is CitySearch</p>
    </div>
  );
}

export default CitySearch;
