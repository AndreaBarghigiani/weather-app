import clsx from "clsx";

function CityTabs({ className }) {
  const classes = clsx(className);
  return (
    <div className={classes}>
      <p>This is CityTabs</p>
    </div>
  );
}

export default CityTabs;
